# TEST_RESULTS — 2026-05-06 23:38

Full-suite test cycle covering unit, integration (with embedded
performance), and soak. Zero failures across all three stages.

## Headline results

| Suite | Result | Duration |
|---|---|---|
| Unit (fast) | **1845 / 1845 pass** | 1:29 |
| Integration + perf (no soak) | **367 / 367 pass** | 5:56 |
| Soak (71 tests across the entire `test_soak_*.py` family) | **71 / 71 pass** | 5:20:43 |
| **Total** | **2283 / 2283 pass** | ~5 h 28 m |

## Triage

**No flakes, no failures.** First cycle in the past several
weeks where every stage passed cleanly on the first run.

The 7 unit-suite arcade-logging flakes that have shown up in
recent cycles (`TypeError: not all arguments converted during
string formatting` from `arcade/sound.py`) did not appear this
run. Likewise no integration / soak flakes — the per-test soak
RSS budget held even with 71 tests sequenced into one Python
process.

## Coverage delta vs prior cycle (2026-05-05 04:13)

- **+102 unit tests** added across the boss-engage + boss-prep
  pipeline + heal-latch follow-up PRs (#41, #43, #45) plus the
  wall-tangent / wall-pin / corner-skip navigation PRs (#42, #44, #46).
  - PR #41: +15 tests (S_ENGAGE_BOSS state routing, kite range,
    station-anchor pull, Phase 2 charge dodge, Phase 3 press,
    QWI staging predicate)
  - PR #43: +47 tests (S_EQUIP_CONSUMABLES / S_PRE_BOSS_MINE /
    S_BUILD_QWI states, action handlers, auto-heal threshold +
    cooldown, bot_builder helpers)
  - PR #45: +11 tests (heal-active latches arm/disarm/fire,
    `_act_at_station` helper success/failure telemetry, dodge
    telemetry)
  - PR #42: +12 tests (wall-tangent escape)
  - PR #44: +13 tests (geometry-aware wall+cluster trap detector)
  - PR #46: +4 tests (corner-skip diagonal escape)
- **No coverage drops** elsewhere — every previously-passing test
  still passes.
- **Soak suite untouched** — same 71 tests as the prior cycle,
  same thresholds, all still well under the 50 MB / 40 FPS budgets.

## PRs shipped this cycle (since prior TEST_RESULTS)

A high-iteration cycle driven by the user's "fight the Double Star
boss end-to-end" goal plus telemetry-driven navigation hardening:

| PR | Title | Trigger |
|---|---|---|
| #34 | Bot: HUNT alien edge-skip + raise mine/gather dwell floor | Telemetry: HUNT pulled into corner aliens |
| #35 | update_logic: shield _tracked_play_sound from audio-backend exceptions | Soak crash from arcade audio backend exception |
| #36 | Bot: suppress HUNT fallback when bot is wall-pinned mid-hunt | Wall-pin trap |
| #37 | Bot: release HUNT when bot is pinned inside building cluster | Cluster-pin trap |
| #38 | Bot: delay HUNT cluster guard to avoid IDLE<->HUNT thrash on entry | Hysteresis tightening |
| #39 | Bot: arm HUNT lockout when pin-escape suppresses target | Pin-escape feedback loop |
| #40 | Bot: skip HUNT cluster guard when bot is wall-pinned | Cluster guard + wall conflict |
| #41 | Bot: ENGAGE_BOSS station-anchor kite + phase-aware Double Star fight | User design discussion: how should the bot fight the boss? |
| #42 | Bot: wall-tangent escape when cluster blocks the inland path | Wall+cluster trap geometry |
| #43 | Bot: post-consumable boss-prep pipeline + auto-heal quick-use loop | User spec: equip consumables, mine to 2000 iron, build QWI, fight boss |
| #44 | Bot: geometry-aware wall+cluster trap force-escape | Position-history stuck-detector defeated by rotation gate |
| #45 | Bot: boss-prep follow-ups -- heal latch, helper, telemetry, docs | Post-PR-#43 review follow-ups |
| #46 | Bot: skip wall-tangent escape at corners (use diagonal instead) | Corner edge case for the wall-tangent escape |

Cumulative impact: the bot autopilot now drives an end-to-end
Double Star boss-prep pipeline (build station → craft modules →
install → craft 25+25 consumables → equip to quick-use slots →
mine to 2000 iron → place QWI → engage boss with phase-aware
station-anchor kite) plus 13 layers of cluster + wall + corner
pin mitigations. Auto-heal latches keep HP / shields topped up
during the entire run.

## Files modified this cycle

| Area | Files |
|---|---|
| Bot autopilot core | `bot_autopilot.py` (+~700 LOC across 13 PRs) |
| Bot autopilot navigation | `bot_autopilot_navigation.py` (wall-pin detector, corner-skip, force-escape) |
| Bot HTTP API | `bot_api.py` (+3 endpoints: /equip_consumables, /use_quick_use, /place_qwi; +quick_use_slots state field) |
| Bot main-thread helpers | `bot_builder.py` (+3 helpers: equip_consumables_to_quick_use, place_quantum_wave_integrator, use_quick_use_slot) |
| Game audio | `update_logic.py` (audio-backend exception shield) |
| Tests | `unit tests/test_bot_autopilot_fsm.py` (+~700 lines), `test_bot_builder.py` (+173), `test_bot_api_state.py` (+29), `test_bot_autopilot_navigation.py` (+~200) |
| Docs | `docs/bot.md` (FSM table, ENGAGE_BOSS section, boss-prep pipeline, auto-heal latch model, max_each cap behavior) |

## Notes for next cycle

- **Live boss-prep run** is the next natural test. The full
  pipeline now exists end-to-end and is unit-tested but hasn't
  been exercised live. Telemetry from a real run will surface any
  state-routing edge cases that the unit tests didn't anticipate.
- **Telemetry payloads grew** with PR #45's nine new event types
  (heal_hp_arm/disarm/fire × HP+shield, equip + build_qwi post
  success/failure, engage_boss_dodge). Review the next
  telemetry session's JSONL volume and consider whether the
  per-tick snapshot interval should widen.
- **Wall+cluster trap mitigations are now 4 layers deep** — wall
  guard exemption, cluster guard wall exemption, force-escape
  detector, corner skip. Worth a follow-up doc consolidating the
  full mitigation stack (currently scattered across 4 PR
  descriptions).
- **All clean cycle** — first time since 2026-04 with zero unit /
  integration / soak failures. The flake floor is now genuinely
  zero rather than "0 once known issues are isolated."

## Effective pass count

**2283 / 2283 tests pass** with zero flakes and zero
regressions. Highest-confidence cycle to date.
