# Test Results — 2026-06-13 12:34 (TERMINATED EARLY)

**Status: INCOMPLETE.** The integration/perf/soak run was **killed at
user request** ~3h05m in, before it finished. The fast unit suite
completed and passed in full. Partial integration results are recorded
below.

| Suite | Result |
|---|---|
| Fast unit (`unit tests/`) | **2704 passed**, 0 failed (179.8s) — COMPLETE |
| Integration + perf + soak (`unit tests/integration/`) | **Terminated at ~67%** after 3:05:21; 1 failure observed (see below) — INCOMPLETE |

Raw logs:
- `_run_unit_2026-06-13_12-34.txt` (complete)
- `_run_integration_2026-06-13_12-34.txt` (partial — buffered to ~67%)

---

## Fast unit suite — COMPLETE, green

```
2704 passed in 179.81s (0:02:59)
```

---

## Integration / performance / soak suite — terminated early

The run was manually stopped (`Stop-Process` on PID 25952) at the user's
direction after ~3h05m. At kill time the process was healthy:
fully CPU-active (11,340 s CPU) with flat memory (1,136 MB RSS, no
leak).

### What the partial output shows

pytest's stdout is line-buffered, so only progress dots up to ~67% were
flushed before termination. Because the process was killed, pytest
never printed its `FAILURES` section or the final summary line.

```
........................................................................ [ 15%]
............................................................F........... [ 31%]
........................................................................ [ 47%]
........................................................................ [ 62%]
.............................                                    (killed here)
```

- **1 failure observed** — a single `F` at the ~31% mark.
- **Identity not captured.** `pytest-randomly` is active, so dot
  position does not map to a stable test across runs, and the
  `FAILURES`/`short test summary` sections never printed. The failing
  test therefore cannot be named from this partial log.

### Triage note

This is the third consecutive full-suite cycle. The two prior cycles
landed a single transient failure in roughly this same region of the
run under heavy machine load (the station-shield Zone 2 perf tests and
the Missile Array + Death Blossom cold-start soak), and **every one was
confirmed passing when re-run in isolation** — i.e. system-contention
artifacts, not defects. The 2026-06-13 05:34 cycle ran to completion
with **459/459 passed and zero failures**, which is the authoritative
recent green result.

Given that, the lone `F` here is most plausibly another load artifact
rather than a regression — but this run was stopped before it could be
identified or re-verified, so that remains **unconfirmed** for this
cycle. No fix was made because no defect was isolated.

### Outstanding

A full integration pass for the 12:34 cycle was not obtained. The most
recent authoritative green integration result is
`TEST_RESULTS_2026-06-13_05-34.md` (459 passed, 0 failed).
